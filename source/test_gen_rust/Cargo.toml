[package]
name = "test_gen_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
schemask = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
schemask = { path = ".." }

[lints.clippy]
all = "allow"
